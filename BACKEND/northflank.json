{
  "name": "civiclens-ai-backend",
  "description": "CivicLens AI FastAPI backend service",
  "runtime": "python",
  "build": {
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn APP.MAIN:app --host 0.0.0.0 --port 8000"
  }
}